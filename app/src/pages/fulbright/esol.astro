---
import Layout from "../../layouts/Layout.astro";
import { PortableText } from "astro-portabletext";
import { getEsolResources } from "../../utils/sanity";

const resources = await getEsolResources();
---

<Layout title="ESOL Tools">
  <h1>ESOL Tools</h1>
  <p>Teaching tools and classroom support resources for ESOL instruction.</p>
  {
    resources.length ? (
      <ul>
        {resources.map((resource) => (
          <li>
            <article>
              <h2>{resource.title || "Untitled ESOL tool"}</h2>
              {resource.description ? <p>{resource.description}</p> : null}
              {resource.body ? <PortableText value={resource.body} /> : null}
              {resource.downloads?.length ? (
                <section
                  aria-label={`${resource.title || "ESOL tool"} downloads`}
                >
                  <h3>Downloads</h3>
                  <ul>
                    {resource.downloads.map((download) => (
                      <li>
                        {download.url ? (
                          <a href={download.url}>
                            {download.label || "Download file"}
                          </a>
                        ) : (
                          <span>
                            {download.label || "Download unavailable"}
                          </span>
                        )}
                      </li>
                    ))}
                  </ul>
                </section>
              ) : (
                <p>No downloads are available yet.</p>
              )}
            </article>
          </li>
        ))}
      </ul>
    ) : (
      <p>No ESOL tools are published yet.</p>
    )
  }
</Layout>
