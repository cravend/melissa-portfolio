---
import type { Image } from "@sanity/types";
import { urlFor } from "../utils/image";

interface Props {
  node: { asset?: { _ref: string }; alt?: string };
}

const { node } = Astro.props;
const asset = node?.asset;
const alt = node?.alt ?? "";
---

{
  asset ? (
    <figure class="my-4">
      <img
        class="mx-auto max-h-[480px] w-auto rounded-lg"
        src={urlFor(node as Image)
          .width(1200)
          .url()}
        alt={alt}
      />
    </figure>
  ) : null
}
