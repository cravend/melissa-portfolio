---
import type { InferGetStaticParamsType } from "astro";
import { PortableText } from "astro-portabletext";
import Layout from "../../layouts/Layout.astro";
import { formatDate } from "../../utils";
import { urlFor } from "../../utils/image";
import { getTravelPost, getTravelPosts } from "../../utils/sanity";

export async function getStaticPaths() {
  const posts = await getTravelPosts();

  return posts.map((post) => ({
    params: { slug: post.slug.current },
  }));
}

type Params = InferGetStaticParamsType<typeof getStaticPaths>;
const { slug } = Astro.params as Params;

const post = await getTravelPost(slug);
---

<Layout title={post.title || "Travel Story"}>
  <article>
    <header>
      <h1>{post.title || "Untitled travel post"}</h1>
      <p>
        <time datetime={post.publishedAt || post._createdAt}>
          {formatDate(post.publishedAt || post._createdAt)}
        </time>
      </p>
      {post.excerpt ? <p>{post.excerpt}</p> : null}
    </header>
    {
      post.mainImage ? (
        <figure>
          <img
            src={urlFor(post.mainImage).url()}
            alt={post.mainImage.alt || post.title || "Travel image"}
          />
        </figure>
      ) : null
    }
    <section>
      <PortableText value={post.body} />
    </section>
  </article>
</Layout>
